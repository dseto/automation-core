# Implementation Notes (MVP)

Este pack é um bootstrap compilável. Próximas melhorias (não implementadas ainda):
- StepTrace + integração com EvidenceManager (steps.log)
- Hooks Reqnroll para captura de evidências on-failure (ScenarioContext.TestError)
- Plan/Validate: suportar mais steps e tags (@quarantine, @custom)
- Waits: melhorar clickability, stale element retries, etc.
- Debug: remover highlight após step (best-effort)
