
# Itens para remover após aplicar o delta

Remover completamente:
- artefatos legados de SpecFlow (se existirem)

Procurar e substituir (não deve sobrar):
- `Automation.SpecFlow` (nome de projeto/pasta legado)
- `namespace Automation.SpecFlow` (em bindings legados)
- referências em docs para caminhos antigos (ex.: `src/Automation.SpecFlow/...`)
